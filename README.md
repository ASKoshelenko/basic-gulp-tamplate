# Basic gulp tamplate
### Installation
Install the dependencies and devDependencies and start the gulp.
```sh
$ git clone project_url
$ cd project_name
$ npm i
```
### Start Gulp
```sh
$ gulp watch
```